# `react-crt-filter`: A simple CRT filter effect as a React Component

Creates a simple CRT filter effect, with flickering, RGB cells, and blur.

## Usage
```
import CrtFilter from 'react-crt-filter';

<CrtFilter>
  {content}
</CrtFtiler>
```
